# defaultContentLanguageInSubdir must be true for this to work, otherwise comment out

# Other languages redirects
[[redirects]]
from = '/fr/**'
to = '/fr/404.html'
status = 404

# Default language must be last.
[[redirects]]
from = '/**'
to = '/en/404.html'
status = 404
